[workspace]
resolver = "3"
members = ["apps/api", "apps/worker"]
exclude = ["apps/api/fuzz", "apps/worker/fuzz", "crates/terminal-plugins"]

[workspace.package]
edition = "2024"
license = "MIT"

[profile.release]
lto = "thin"
strip = "symbols"
